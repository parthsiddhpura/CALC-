<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:background="@drawable/widget_bg_glass"
    android:gravity="center_vertical"
    android:padding="10dp">

    <!-- Expression & Result (e.g. ₹25,000 + 18% = ₹29,500) -->
    <LinearLayout
        android:id="@+id/btn_open_calc"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/widget_compact_expr"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="₹25,000 + 18%"
            android:textColor="#94A3B8"
            android:textSize="11sp"
            android:singleLine="true" />

        <TextView
            android:id="@+id/widget_compact_result"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="= ₹29,500"
            android:textColor="#00E5FF"
            android:textSize="17sp"
            android:textStyle="bold"
            android:singleLine="true" />
    </LinearLayout>

    <!-- Quick Action Launch Button -->
    <TextView
        android:id="@+id/btn_quick_launch"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:background="@drawable/widget_btn_accent_glass"
        android:gravity="center"
        android:paddingHorizontal="10dp"
        android:text="CALC ↗"
        android:textColor="#00E5FF"
        android:textSize="11sp"
        android:textStyle="bold" />
</LinearLayout>
